public interface Settings {
    int windowWidth = 500;
    int windowHeight = 500;
    int dimension = 5;
    int iconSize = windowWidth / dimension - (int)(((double) windowWidth / dimension) * 0.2);
}
